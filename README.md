# KB Payee Guard

**해외송금 신청 시 이미 제출된 계약서를 AI가 읽어, 이번 계좌 지시가 계약이 정한 변경 절차를 따랐는지 대조하고 어긋나면 송금을 보류하는 게이트.**

제8회 KB A.I Challenge · 금융 자유주제 · 팀 aithor

```
python3.12 -m unittest discover -s tests     # 설치 없이 바로 실행됩니다 (122건, 1초 미만)
```

---

## 30초 요약

무역 대금을 보내는 기업에 *"거래 은행이 바뀌었으니 새 계좌로 보내주세요"* 메일이 옵니다.
3년 거래한 담당자 이름, 인보이스 첨부. 송금합니다. 3주 뒤 상대가 말합니다 — **"대금이 안 들어왔는데요."**

국내 **무역송금 사기 접수** 약 1,600건 (2021~2025 상반기, 금감원 집계 · [KBS 보도](https://news.kbs.co.kr/news/pc/view/view.do?ncd=8547381)).
건당 손실은 **대금 전액**이고, 해외 계좌로 나간 자금이라 **회수는 사실상 0**입니다.

이 사기를 막는 제품은 이미 여럿 있습니다. 전부 **"이 계좌번호가 이 회사 것이 맞나"** 를 확인합니다.
그래서 사기범이 **거래처와 같은 법인명으로 계좌를 개설하면 통과**합니다 —
유사 상호 법인 설립이나 계정 탈취 후 명의 도용으로 실제 일어나는 일이고, **벤더 자신이 인정합니다**(아래 인용).

**KB Payee Guard는 계좌번호를 검증하지 않습니다. 그 계좌번호를 알려준 경로를 검증합니다.**

> 계좌번호는 바꿀 수 있어도, 3년 전 계약서에 적힌 *"변경은 양측 서면 합의로"* 는 바꿀 수 없습니다.

---

## 한 장으로 보는 동작

```
 제출된 계약서 (2023-04-11)              이번 송금 신청
   상대방    BAUER GmbH                    수취계좌   LT12 … 47   🇱🇹
   소재지    Stuttgart, Germany 🇩🇪         송금방식   T/T
   결제조건  Irrevocable L/C                계좌 지시  이메일 한 통
   §14 변경  "서면 합의로만"
                    │                            │
                    └───────────  대조  ──────────┘
                                   ↓
   ▸ 계약서 §14는 계좌 변경을 서면 합의로 제한합니다.          ← S16 (판정 축)
     이번 지시는 이메일 한 통입니다.
   ▸ 계약 상대방은 독일인데 계좌 개설국이 리투아니아입니다.     ← S9  (금감원 지시 항목)
   ▸ 계약서는 L/C 결제인데 이번엔 T/T입니다.                   ← S10
     L/C는 은행이 지급을 보증하지만 T/T는 계좌로 바로 들어갑니다.
                                   ↓
                        송금 보류 · 사람 승인 요구
```

사용자가 **앱을 켤 필요가 없습니다.** 별도 제품이 아니라 은행 송금 신청 흐름 안에 있어서,
속은 줄 모르는 피해자도 반드시 거칩니다. **추가 서류도 요구하지 않습니다** —
외국환거래법상 **연 10만 달러 초과 송금**은 계약서 제출이 이미 의무입니다.
→ 즉 이 게이트의 **1차 적용 범위는 계약서가 제출되는 고액 무역송금**입니다. 그 아래 구간은 대상이 아닙니다.

> 은행은 그 계약서로 *"정당한 거래 목적인가"*(자금세탁 방지)를 확인합니다.
> 그런데 **그 계약서가 정한 계좌 변경 절차와 이번 송금이 일치하는지는 확인하지 않습니다.**
> 서류는 이미 옵니다. 아무도 읽지 않을 뿐입니다.

---

## 실제로 돌린 결과

위 다이어그램과 같은 케이스를 판정 엔진에 그대로 넣은 **실행 출력**입니다.
아래 코드를 복사해 붙이면 같은 결과가 나옵니다 (API 키 불필요).

<details>
<summary>입력 (계약서 사실 + 송금 신청) — 클릭</summary>

```python
facts = ContractFacts(
    counterparty_name="BAUER GmbH", counterparty_country="DE",
    registered_contact=["ap@bauer-gmbh.de"],
    payment_terms=PaymentTerms.LC, contract_date="2023-04-11",
    notice_channel_types=["POSTAL", "EMAIL"],
    amendment_clause="§14.2 Any change of bank account shall be effective "
                     "only upon written agreement of both parties.",
    amendment_clause_requires_written=True,
)
req = RemittanceRequest(
    case_id="DEMO-001", payee_name="BAUER GmbH",
    new_account=Account(number="LT121000011101001047", bic="REVOLT21"),   # 🇱🇹
    amount=Money(184500.0, "EUR"), remittance_type=PaymentTerms.TT,       # 계약은 L/C
    account_instruction=AccountInstruction(source=InstructionSource.EMAIL,
                                           channel_detail="ap@bauer-gmbh.co"),
    has_contract=True, change_request_sender="ap@bauer-gmbh.co",          # .de → .co
)
rules.evaluate(facts, req)
```

</details>

**사기 케이스 — 차단**

```
verdict : Verdict.BLOCK_PENDING   rule: R2

  - 계약서는 계좌 변경을 서면 합의로 제한하는데 이번 지시는 이메일입니다.
  - 계약 상대방 소재국은 DE인데 수취계좌 개설국은 LT입니다.
  - 계약서 결제조건은 L/C(화환신용장)인데 이번 송금은 T/T(전신송금)입니다.
    L/C는 은행이 지급을 보증하지만 T/T는 계좌로 바로 들어갑니다.
  - 발신 도메인 'bauer-gmbh.co'이 계약서 기재 도메인 'bauer-gmbh.de'과 유사하지만 다릅니다.
  - 계좌 변경 요청 메일이 기존 대화 스레드에 이어지지 않습니다.
```

**정상 계좌변경 — 통과** (같은 계약서, 수정 합의서로 온 독일 계좌 · L/C 유지)

```
verdict : Verdict.PASS            rule: R0
```

이 문장들이 **LLM이 쓴 것이 아닙니다.** 규칙 테이블이 조건을 그대로 문장으로 낸 것이라,
같은 입력이면 항상 같은 문장이 나옵니다. 사용자에게 보이는 설명에 모델이 개입할 여지가 없습니다.

> ⚠️ **현재 범위**: CLI·라이브러리 코어까지입니다. REST API·송금 신청 화면·은행 시스템 연동은
> 미구현이며 본선 구간입니다 (로드맵 P7).

---

## 왜 기존 방식으로는 안 되는가

상용 제품 7종 · EU 법정 제도 · 국내 은행 2곳 · 6개 은행 업무 매뉴얼을 조사했습니다
([조사노트 §E-3](docs/00_조사노트.md)). **조사한 범위 안에서는 공통점이 하나입니다 —
전부 "계좌라는 값"을 검증합니다.** 값은 사기범이 통제할 수 있습니다.

| 이미 있는 것 | 무엇을 검증하나 | 계정 탈취 + 같은 나라·같은 법인명이면 |
|---|---|---|
| Proofpoint · Trend Micro | 메일 도메인·문체 | ❌ 도메인이 진짜라 통과. 애초에 송금을 못 막음 |
| Trustpair · Eftsure · Trustmi | 벤더 마스터 계좌 이력 | ⚠️ 변경은 감지하나 **정당/사기를 못 가름**. ERP 전용(대기업) |
| iPiD · **EU VoP** (2025-10 법정) | 이름 ↔ 계좌 | ❌ **법인명으로 계좌를 열면 통과.** EU 역내 유로만 |
| 하나·우리은행 수취계좌 사전확인 | 이름·계좌·BIC | ❌ 동일 + 영업점 방문 + 사용자 능동 요청 |
| 국내 6개 은행 업무 매뉴얼 (2018~) | 계좌 변경 시 유선 확인 | ⚠️ 방향은 맞으나 **사람 판단**, 계약서 대조 없음 |
| **KB Payee Guard** | **계좌 지시가 계약 절차를 따랐는가** | ✅ **계약서 §Amendment는 사후 위조 불가** |

한계를 벤더 자신이 인정합니다:

> *"사기범이 공급업체의 **정확한 법인명으로 계좌를 개설하면 이름 대조는 통과**한다."*
> — [Eftsure](https://www.eftsure.com/blog/products/top-6-bank-account-verification-software-in-2025/)
>
> *"VoP는 이름이 계좌와 일치하는지 확인하지만 **신분 도용은 탐지되지 않는다**."*
> — [Trustpair](https://trustpair.com/blog/verification-of-payee/)

그리고 **금감원이 전 은행에 지시한 항목**이 우리 신호와 그대로 겹칩니다:

> *"**수취인 국가와 수취 은행 소재 국가가 서로 다르거나** 계좌번호가 평소와 다른 경우
> **다시 확인하는 절차**를 거치도록"* — [KBS](https://news.kbs.co.kr/news/pc/view/view.do?ncd=8547381)

---

## 판정 축 — S16

가장 흔한 BEC는 **국가도 이름도 결제조건도 전부 그대로**입니다. **계좌번호만** 바뀝니다.
그 케이스에서 다른 신호는 전부 침묵합니다. 남는 질문은 하나뿐입니다:

> ### "이 계좌 지시가 계약이 정한 경로로 왔는가?"

계약서 §Notices·§Amendment는 **체결 시점에 확정되어 사기범이 사후에 바꿀 수 없습니다.**
메일 주소는 위조해도 3년 전 계약서 조항은 위조할 수 없습니다.
그리고 **은행은 송금 신청 시 "이 계좌 정보를 어디서 받으셨습니까"를 이미 묻습니다** — 사용자 부담 0.

| ID | 신호 | 신규성 |
|---|---|---|
| **S16** | **계좌 지시 출처 ≠ 계약이 정한 절차** (§Notices·§Amendment) | **최빈 BEC를 잡는 유일한 축** |
| S9 | 계약 당사자 소재국 ≠ 수취계좌 개설국 | 금감원 지시 항목 |
| S10 | 계약서 결제조건 ≠ 실제 송금방식 (L/C→T/T) | 기존 제품 사용 확인 0건 |
| S11 | 계약서 수익자명 ≠ 수취인명 | EU VoP와 중복 |
| S1 | 신규 수취계좌 | **트리거일 뿐 판별자 아님** — 정상 변경도 이력이 없음 |

S10은 **방향을 구분합니다.** T/T→L/C(더 안전한 방향)는 경고하지 않습니다.
무조건 경고하면 현장에서 게이트가 꺼집니다.

---

## AI가 없으면 이 제품은 성립하지 않습니다

S16은 `[계약이 허용한 경로] ∩ [이번 지시가 온 경로]`를 계산합니다.
오른쪽은 은행이 폼으로 묻지만, **왼쪽은 계약서 자연어에만 있습니다.**

실물 계약서 **30건 전수 측정** ([`docs/05`](docs/05_베이스라인A_실측.md)):

| 조항 추출기 | 정확도 | Recall |
|---|--:|--:|
| 정규식 | 30.0% | **20.8%** |
| **LLM (gpt-4o-mini)** | **90.0%** | **91.7%** |

정규식은 해석에서 지기 전에 **조항을 찾지 못합니다** — 오답 21건 중 19건이 미탐지.
조항이 표준 위치에 있지 않고 본문 문단에 섞여 있기 때문입니다.

**그렇다고 LLM에 판정을 맡기지는 않습니다.** 계약서·인보이스·메일은 전부 사기범이 작성할 수 있는
입력이라, LLM에 등급 결정권을 주면 문서에 삽입된 지시로 뚫립니다.

```
LLM  →  계약서에서 사실만 추출 (추출 스키마에 판정 칸이 아예 없음)
규칙 →  등급과 근거 문장 결정 (결정론 테이블 11개)
```

---

## 게이트가 실제로 무엇을 막고 무엇을 통과시키나

실물 계약서 30건에 **자체 설계 시나리오 5종**을 적용한 150건 전수 **기능시험**입니다
([`docs/06`](docs/06_E2E_탐지율_오탐률.md)). 실제 사건 데이터가 아니고, 독립 표본 30건이
아니라 **같은 계약서 30건의 반복 적용**입니다.

| 추출 경로 | 설계 의도대로 차단 | 정상 시나리오 오차단 |
|---|--:|--:|
| LLM | 93.3% | 0.0% |
| 정규식 | 16.7% | 0.0% |

이 30건은 **규칙을 고치는 데도 쓴 데이터**입니다. 그래서 아래를 따로 측정했습니다.

### 🎯 Holdout — 규칙 튜닝에 한 번도 안 쓴 232건

저장소의 계약서는 262건인데 위 평가는 그중 30건만 씁니다.
**나머지 232건**(CUAD 46 · 무역 영문 62 · 국문 49 · SEC 75)은 규칙 튜닝에 쓰인 적이 없습니다.
같은 시나리오 5종을 그대로 합성해 돌렸습니다 — `scenarios.build()`는 정답 라벨이 아니라
계약서 사실에서 시나리오를 만들기 때문에 라벨 없는 계약서에도 그대로 적용됩니다.

| 코퍼스 | 계약서 | 시나리오 | 설계 의도대로 차단 | **정상 오차단** |
|---|--:|--:|--:|--:|
| gold (규칙 튜닝에 **사용**) · LLM | 30 | 150 | 93.3% | 0.0% (0/60) |
| **holdout (**미사용**) · LLM** | **232** | **1,160** | **75.0%** | **0.0% (0/464)** |
| holdout · 정규식 | 232 | 1,160 | 18.1% | 0.0% (0/464) |

```bash
python3.12 scripts/run_e2e.py --corpus holdout --extract C   # 232건 · 약 10분 · 약 $0.17
python3.12 scripts/run_e2e.py --corpus holdout --extract A   # 키 불필요 · 약 5초
```

**오탐률은 464건에서 0%가 유지됩니다** — gold의 60건보다 8배 큰 표본입니다.
"정상 거래를 막지 않는다"는 현장 도입의 1차 관문이고, 여기서는 일반화가 확인됐습니다.

### 🔴 그런데 차단율은 93.3% → 75.0% 로 떨어졌습니다

**평균만 보면 오해합니다.** 계약서 종류별로 쪼개면 한 곳에서만 무너집니다:

| 코퍼스 | 계약서 | 차단율 | |
|---|--:|--:|---|
| 무역 영문 | 62 | **98.4%** | 실제 타깃 문서군 |
| 미국 상용 (CUAD) | 46 | 91.3% | gold 30건과 같은 종류 |
| SEC 공시 | 75 | 89.3% | |
| **국문 표준서식** | **49** | **🔴 8.2%** | **여기만 무너집니다** |

**국문 공격 98건 중 90건이 `R1 UNKNOWN`** — 차단 실패가 아니라 **판정 불가**입니다.
막지 못한 게 아니라 **판단할 근거가 계약서에 없었습니다.** 원인은 이미 측정돼 있었습니다
([`docs/03`](docs/03_R7_측정.md) §3-bis):

```
국문 계약서 49건 —  §Notices 보유율 26.5%  ·  이메일 기재율 0.0%
```

S16은 *"이 지시가 계약이 정한 경로로 왔는가"* 를 묻는데, **국문 표준서식에는 그 "경로"가
아예 안 적혀 있습니다.** 판정 축 자체가 존재하지 않으니 UNKNOWN이 맞습니다 —
통과시키지 않고 보류한다는 점에서 INV-5는 지켜졌습니다.

🔴 **한국 은행에 파는 제품인데 국문에서 8.2%입니다.** 이게 이 프로젝트의 가장 큰 미해결
문제이고, 본선 최우선 과제입니다(로드맵 P10). 실물 국문 무역계약서 확보가 먼저입니다 —
현재 49건은 전부 **국가법령정보센터 표준서식**이고 실물은 0건입니다.

정규식으로 추출하면 **공격의 83%가 통과합니다** — 게이트가 사실상 꺼집니다.

**알려진 사각지대를 평가셋에서 빼지 않았습니다.** 사기범이 **수정 합의서를 위조**하면
통과합니다(30건 전건 미탐). S16은 지시의 *경로*를 보지 문서의 *진위*를 보지 않기 때문입니다.
통과할 것을 알면서 넣었습니다 — 빼면 숫자는 좋아지지만 심사에서 물었을 때 답이 없습니다.

---

## 직접 돌려보기

### 1단계 — API 키 없이 (여기부터)

```bash
git clone https://github.com/Aithor-organization/kb-payee-guard.git
cd kb-payee-guard
python3.12 -m unittest discover -s tests
```

```
Ran 122 tests in 0.3s

OK
```

**`pip install`이 없습니다.** 코어가 stdlib only라 의존성이 0입니다.
판정 규칙 · 승인 게이트 · 인젝션 방어 · dead rule 검사가 전부 여기서 검증됩니다.
(macOS 8회 실측 0.28~0.47초 — 머신마다 다르므로 초를 못 박지 않습니다.)

```bash
python3.12 scripts/run_e2e.py --extract A   # 정규식 경로 탐지율 (키 불필요)
```

### 2단계 — OpenAI API 키를 넣고 LLM 추출까지 재현하기

**1단계는 키가 필요 없습니다.** 여기부터는 계약서에서 조항을 실제로 뽑는 경로(C)라
OpenAI 키가 필요합니다. 아래 그대로 따라 하시면 됩니다.

#### ① 키 발급

[platform.openai.com/api-keys](https://platform.openai.com/api-keys) → **Create new secret key**
→ `sk-` 로 시작하는 문자열을 복사합니다. (결제수단 등록이 되어 있어야 호출이 됩니다.)

#### ② 키 등록 — 둘 중 편한 쪽

```bash
# 방법 1. 저장소 루트에 .env 파일  (권장 — .gitignore 에 있어 커밋되지 않습니다)
cd kb-payee-guard
echo 'OPENAI_API_KEY=sk-여기에-키를-붙여넣으세요' > .env

# 방법 2. 환경변수  (현재 셸에서만 유효)
export OPENAI_API_KEY="sk-여기에-키를-붙여넣으세요"
```

> 🔒 `.env` 는 `.gitignore` 에 등재되어 있습니다 — `git check-ignore -v .env` 로 확인 가능합니다.
> 키를 코드나 커밋에 넣지 마세요.

#### ③ 등록 확인

```bash
python3.12 -c "import os,pathlib
[os.environ.setdefault(*l.split('=',1)) for l in pathlib.Path('.env').read_text().splitlines() if '=' in l]
k=os.environ.get('OPENAI_API_KEY','')
print('✅ 키 인식됨' if k.startswith('sk-') else '🔴 키 없음')"
```

#### ④ 실행

```bash
python3.12 scripts/run_baselines.py --with-c              # 추출 정확도 A vs C  (30건 · 약 1분)
python3.12 scripts/run_e2e.py --extract C                 # 탐지율·오탐률       (150건 · 약 3분)
python3.12 scripts/run_e2e.py --corpus holdout --extract C # holdout 재측정      (232건 · 약 10분)
```

#### 💰 비용 — 생각보다 훨씬 쌉니다

| 실행 | 계약서 | 실제 전송 | 비용 |
|---|--:|--:|--:|
| `run_baselines.py --with-c` | 30 | 0.4 MB | **약 $0.02** |
| `run_e2e.py --extract C` | 30 | 0.4 MB | **약 $0.02** |
| `run_e2e.py --corpus holdout --extract C` | 232 | 2.9 MB | **약 $0.17** |

계약서를 통째로 보내지 않기 때문입니다 — `max_chars=24,000` 으로 **§Notices·§Amendment
주변만 발췌**해 보냅니다. 390KB 계약서도 24K자만 전송되고, 원본 12.8MB 중 **22%만** 나갑니다.
모델은 `gpt-4o-mini`(입력 $0.15/1M · 출력 $0.60/1M)입니다.
`[추정]` — 실측 전송 문자수 × 4자/토큰 가정 × 공개 단가. 정확한 값은 OpenAI Usage 페이지에서 확인하세요.

#### 키가 없거나 잘못됐을 때

조용히 잘못된 결과를 내지 않고 **멈춥니다**:

```
LLMProviderError: no API key (set OPENAI_API_KEY or OPENAI_KEY_FILE)
```

> **1단계 122건은 키 없이 그대로 통과합니다.** 심사에 필요한 안전 불변식(승인 게이트 ·
> 인젝션 방어 · dead rule 검사)은 전부 1단계에 있습니다.

> ⚠️ `run_e2e.py`는 실행할 때마다 결과 파일을 덮어씁니다 —
> `--corpus gold` 는 `_e2e_result.json`, `--corpus holdout` 은 `_e2e_holdout_{A,C}.json` 입니다
> (추출기별로 분리되어 A 결과를 C 실행이 덮지 않습니다).
> 커밋된 `_e2e_result.json` 은 `gold`(추출 완벽 시 상한) 결과입니다.

---

## 설계 원칙

| 원칙 | 내용 |
|---|---|
| 켜지 않아도 작동 | 별도 앱이 아니라 **송금 신청 흐름 안**에 있음 |
| 판정이 아니라 대조 | 확률을 말하지 않음. *"계약서에 뭐라 적혔고 이번 송금은 어떻다"* 만 말함 |
| 추가 서류 요구 0 | 법정 제출 서류만 사용 |
| LLM은 판정하지 않음 | 등급은 규칙 테이블이 결정 — 프롬프트 인젝션 방어 |
| 계약 체결 시점이 신뢰 기준 | §Notices·§Amendment·기재 연락처 전부 사후 변경 불가 |
| 모든 규칙은 발화해야 함 | 입력이 없는 규칙은 배포 금지 — 도달성 전수 검사 |

**튜닝할 계수가 없습니다.**

```
❌ "이 변경은 사기 확률 87%"                              → 정답 라벨 필요, 가중치 튜닝 필요
✅ "계약서 §14는 서면 합의를 요구하는데 이번 지시는 이메일"  → 두 문서에서 직접 나오는 사실
```

규칙의 **조건이 곧 사용자에게 보이는 문장**입니다. 조정할 가중치도 임계값도 없습니다.
대신 **공격 테스트 16종**으로 *"이 조합은 대조로 걸린다"* 를 증명합니다.

> 단 *"평가가 필요 없다"* 는 뜻은 아닙니다. 판정이 결정론이어도 **입력(계약서 추출)이 틀리면
> 결과는 확정적으로 틀립니다.** §Notices·§Amendment 추출 정확도가 시스템 정확도의 상한입니다.

---

## 구현 현황

**판정 엔진 · 계약서 판독기 · 승인 게이트가 동작합니다** (2026-08-02).

| 층 | 내용 | 상태 |
|---|---|:---:|
| L1 판독 | A(정규식) · **C(LLM, strict json_schema)** · Form | ✅ |
| L2 대조 | S9 · S10 · S11 · S12 · **S16**(판정 축) | ✅ |
| L4 규칙 | R1~R9 · R11 · R0 (11개) + 도달성 전수 검사 | ✅ |
| **L6 승인 게이트 + 원장** | 1회용 · 건·계좌 결박 · TTL · 채널 화이트리스트 | ✅ |
| 감사 로그 | `trace_id` 전 구간 전파 · append-only | ✅ |
| 안전 | 인젝션 3중 방어 · 근거 구간 원문 대조 | ✅ |
| L5 근거 서술 (LLM) | 설계만 — **현재는 규칙이 낸 결정론 문장을 사용** | ⬜ |
| L3 보조 신호 M3·M4·M6·M7·M8 | M1·M2만 구현 | ⬜ |
| 시뮬레이터 UI · REST API | 본선 구간 | ⬜ |

**승인 게이트는 코드로 강제합니다.** 게이트 함수는 승인 *객체*를 받지 않고 **`approval_id` 문자열만**
받아 원장을 조회합니다. 모델이 승인을 위조하려면 원장에 써야 하는데, 원장에 쓰는 유일한 경로는
**사람이 검증된 채널로 확인한 발급 함수**입니다. 공격 테스트 T1~T5·T10·T11이 이를 검증합니다.

> **L5는 미구현입니다.** 현재 사용자에게 나가는 근거 문장은 규칙이 만든 결정론 문자열입니다
> (아래 실행 예시의 출력이 그것입니다). LLM 서술을 붙이면 표현은 자연스러워지지만
> **문서에 삽입된 지시가 사용자 설명까지 오염시킬 경로가 생기므로**, 인젝션 방어를 먼저
> 검증한 뒤 붙입니다 — 미룬 것이지 빠뜨린 것이 아닙니다.

> **R10은 없습니다** — 2026-08-02에 제거했습니다. S1(신규 계좌) 단독 차단이
> **정당한 계좌 변경을 100% 막았기** 때문입니다. 실제 규칙은 11개입니다.

---

## 한계 — 숨기지 않은 것

심사에서 물어보실 만한 것을 먼저 적습니다.

| 항목 | 상태 |
|---|---|
| **수정 합의서 위조** | 🔴 통과합니다 (30건 전건 미탐). S16은 경로를 보지 진위를 보지 않습니다 |
| **S10 기저율** | 🔴 **측정 안 함.** 정상 무역에서 L/C→T/T가 흔하면 오탐이 됩니다 — 한 번도 묻지 않았습니다 |
| **탐지율 수치의 성격** | ⚠️ **자체 설계 시나리오** 기준이고, 실제 BEC 사건 데이터가 아닙니다. 단 **holdout 232건에서 성능이 유지**되는 것은 확인했습니다(위) |
| **국문 계약서 차단율** | 🔴 **8.2%** (holdout 49건). §Notices 26.5% · 이메일 0.0% 라 판정 축이 문서에 없습니다 — 본선 최우선 과제 |
| **holdout 일반화** | 🟡 무역 98.4% · 미국 91.3% · SEC 89.3% 로 유지되나 **국문이 평균을 75.0%로 끌어내립니다** |
| **국문 계약서 이메일 기재율** | 🔴 **0.0%.** S16-b(발신 이메일 대조)가 국문에서는 대조 대상 자체가 없습니다 |
| **§Notices 보유율** | 무역 영문 51.6% · 국문 26.5% — 조항이 없으면 판정이 중위험으로 떨어집니다 |
| **국문 표본** | 🟡 49건 전부 표준서식이고 **실물은 0건** |
| **AST 정적 검사기** | ⬜ 미구현. 판정 모듈이 순수 함수라 사실상 만족하나 기계 검증은 없습니다 |

측정을 마친 것: **R-7 계약서 조항 존재율**(실물 151건) · **A/C 추출 정확도**(30건) ·
**오탐률**(정상 시나리오 2종에서 0%, 단 실제 트래픽 다양성은 훨씬 넓습니다).

자체 측정이 틀렸던 경위까지 기재했습니다 — gold set 라벨 8건 오류를 사후 정정
([`docs/05` §1](docs/05_베이스라인A_실측.md)).

---

## 로드맵

| 단계 | 산출물 | 상태 | 완료 기준 | 목표 |
|---|---|:---:|---|---|
| P1 | 워크플로우 + HITL 게이트 + 승인 원장 | ✅ | 공격 테스트 T1~T5·T10·T11 통과 | 완료 (8/2) |
| P2 | L1 계약서 판독 + UNKNOWN 경로 | ✅ | 추출 Recall 91.7% 달성 | 완료 |
| P3 | L2 대조 (S16·S9·S10·S11·S12) | ✅ | 150건 전수 평가 실행 | 완료 |
| P4 | **L4 규칙 11개 + 도달성 검사** | ✅ | dead rule 0건 (T15) | 완료 |
| P5 | L3 보조 신호 M3·M4·M6·M7·M8 | ⬜ | M6·M8 주입 시 판정 변화 시연 | 8월 2주 |
| P6 | L5 근거 서술 (LLM) | ⬜ | 근거 문장이 원문 구간에 100% 매핑 | 8월 3주 |
| P7 | 송금 신청 화면 시뮬레이터 + REST API | ⬜ | 데모 10종 화면 재현 | 8월 4주 |
| P8 | **평가셋 분리** (holdout 232건) | ✅ | A·C 양 경로 완료 — 오탐 0% 유지, 차단 75.0% | 완료 (8/3) |
| **P10** | **국문 계약서 대응** | ⬜ | 🔴 실물 국문 무역계약서 확보 후 국문 차단율 **50%+** | 본선 최우선 |
| P9 | 실사건 데이터 확보 | ⬜ | 익명화 BEC 사례로 재측정 | 본선일(9/2) |

> 🔴 **P8이 P10을 만들어냈습니다.** holdout을 돌리지 않았으면 "93.3%"만 들고 갔을 것이고,
> **국문에서 8.2%라는 사실을 심사장에서 처음 들었을 것**입니다. 이게 holdout을 돌린 이유입니다.

**막는 것을 먼저 만들고, 판단하는 것을 나중에 붙였습니다.**
안전 불변식 없이 판정 로직을 쌓으면 나중에 게이트를 끼울 때 우회 경로가 남습니다.

> **P2가 P3의 상한을 정합니다.** §Notices·§Amendment를 못 읽으면 S16이 전부 중위험으로
> 떨어져 모든 건이 보류 — 게이트가 꺼집니다.

---

## 문서

| 문서 | 내용 |
|---|---|
| [`docs/00_조사노트.md`](docs/00_조사노트.md) | 검증된 사실 · 실측 16절 · 미확인 항목 · **불리한 발견** |
| [`docs/01_MVP제안서.md`](docs/01_MVP제안서.md) | 문제 · 경쟁 지형 · 규제 근거 · 데모 10종 · 심사 Q&A 10개 |
| [`docs/02_기술명세서.md`](docs/02_기술명세서.md) | 불변식 8종 · 신호 체계 · 규칙 11개 · API · 공격 테스트 16종 |
| [`docs/03_R7_측정.md`](docs/03_R7_측정.md) | 계약서 조항 존재율 실측 (실물 151 + 무역 62 + 국문 49건) |
| [`docs/05_베이스라인A_실측.md`](docs/05_베이스라인A_실측.md) | 정규식 vs LLM 추출 정확도 (30건 전수) |
| [`docs/06_E2E_탐지율_오탐률.md`](docs/06_E2E_탐지율_오탐률.md) | 탐지율·오탐률 (150건 전수) |
| [`workflows/kb-payee-guard.workflow.yaml`](workflows/kb-payee-guard.workflow.yaml) | 워크플로우 정의 (12 nodes) |
| [`compliance_packs/trade-remittance.json`](compliance_packs/trade-remittance.json) | 15 controls · 규제 근거 2건 매핑 · 공격 테스트 16 · 데모 10 |

---

## 기술 스택

**AITHOR-Agent-Framework** 위에 도메인 로직만 얹었습니다. 워크플로우 스키마의
`kind: deterministic`이 *"LLM은 판정하지 않는다"* 를 강제하고, 내장 guard가
입력·도구결과·출력을 3중 검문합니다.

**다만 이 제출물은 프레임워크 없이 단독으로 돕니다.** 위 122건은 프레임워크를 설치하지 않은
환경에서 그대로 통과합니다 — **심사 재현에 비공개 저장소가 필요 없습니다.**

<details>
<summary>프레임워크 자체 테스트 결과 (2026-08-02 macOS 실측)</summary>

| 인터프리터 | 통과 | 실패 | skip | 시간 |
|---|---:|---:|---:|---:|
| Python 3.12 | 1,264 | 3 | 22 | 34.6초 |
| Python 3.11.5 (pytest 9.0.2) | 1,249 | 17 | 23 | 38.9초 |

실패는 전부 환경·격리 문제이고 도메인 로직과 무관합니다 [검증됨]:
- 샌드박스 OS 격리 **3건**(양쪽 공통) — Linux 전용 `RLIMIT_AS`·네임스페이스 의존이라 macOS 실패
- async **14건**(3.11에서만) — 해당 2개 파일만 단독 실행하면 **19건 전부 통과**.
  전체 실행 시의 테스트 간 이벤트 루프 오염이지 기능 결함이 아닙니다

</details>

| 영역 | 선택 |
|---|---|
| 코어 | Python 3.12 · **stdlib only, 의존성 0** |
| 테스트 | `unittest` (표준 라이브러리) · 122건 |
| 조항 추출 | OpenAI `gpt-4o-mini` · strict `json_schema` |
| 판정 | 결정론 규칙 테이블 (LLM 판정 권한 0) |

---

<sub>제8회 KB A.I Challenge 출품작 · 현직자 Pick #6 "수출입 금융 지원 에이전트"와 무대는 겹치나
Pick #6은 *정보 제공·추천*, 본 과제는 *송금 차단 게이트*라 결이 달라 자유주제로 출품합니다
(2026-07-31 공고 원문 대조).</sub>
